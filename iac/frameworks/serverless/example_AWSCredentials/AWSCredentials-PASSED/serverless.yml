service: my-sls-service

provider:
  name: aws
  runtime: python3.7
  stackName: lambda-${self:service.name}
  tag: ${opt:tag}
  environment:
    TABLE_NAME: "mytable"
    BUCKET_NAME: "mybucket"
  iamRoleStatements:
    - Effect: "Allow"
      Action:
        - "s3:PutObject"
        - "s3:GetObject"
      Resource:
        - "arn:aws:s3:::somebucket/*"

functions:
  myFunc:
    environment:
      FUNC_NAME: "myfunc"
    name: myFunc
    tags:
      RESOURCE: lambda
      PUBLIC: false
      yor_trace: 673e1275-5759-4e78-b82f-ff5ed3b66e0a
    iamRoleStatements:
      - Effect: Allow
        Action:
          - "lambda:InvokeFunction"
        Resource:
          - "arn:aws:lambda:#{AWS::Region}:#{AWS::AccountId}:function:invokedLambda"
    handler: Handler.handle
    timeout: 600
    memorySize: 320